//jshint esversion:6

const express = require("express");
const bodyParser = require("body-parser");
const ejs = require("ejs");
const mongoose = require('mongoose');

const homeStartingContent = "Introducing a dynamic and engaging blog website that provides readers with high-quality content on a variety of topics. Our blog website is designed to be user-friendly and easy to navigate, with a clean and modern layout that appeals to a wide range of readers";
const aboutContent = "Introducing a dynamic and engaging blog website that provides readers with high-quality content on a variety of topics. Our blog website is designed to be user-friendly and easy to navigate, with a clean and modern layout that appeals to a wide range of readers";
const contactContent = "Introducing a dynamic and engaging blog website that provides readers with high-quality content on a variety of topics. Our blog website is designed to be user-friendly and easy to navigate, with a clean and modern layout that appeals to a wide range of readers";

const app = express();

app.set('view engine', 'ejs');

app.use(bodyParser.urlencoded({extended: true}));
app.use(express.static("public"));

mongoose.connect("mongodb://localhost:27017/blogDB", {useNewUrlParser: true});

const postSchema = {
  title: String,
  content: String
};

const Post = mongoose.model("Post", postSchema);

app.get("/", function(req, res){

  Post.find({}, function(err, posts){
    res.render("home", {
      startingContent: homeStartingContent,
      posts: posts
      });
  });
});

app.get("/compose", function(req, res){
  res.render("compose");
});

app.post("/compose", function(req, res){
  const post = new Post({
    title: req.body.postTitle,
    content: req.body.postBody
  });


  post.save(function(err){
    if (!err){
        res.redirect("/");
    }
  });
});

app.get("/posts/:postId", function(req, res){

const requestedPostId = req.params.postId;

  Post.findOne({_id: requestedPostId}, function(err, post){
    res.render("post", {
      title: post.title,
      content: post.content
    });
  });

});

app.get("/about", function(req, res){
  res.render("about", {aboutContent: aboutContent});
});

app.get("/contact", function(req, res){
  res.render("contact", {contactContent: contactContent});
});


app.listen(3000, function() {
  console.log("Server started on port 3000");
});
